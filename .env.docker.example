# ============================================
# Docker 部署环境变量配置
# Docker Deployment Environment Variables
# ============================================
# 使用方法 / Usage:
# 1. 复制此文件为 .env.docker
#    Copy this file to .env.docker
# 2. 修改下面的配置值
#    Modify the values below
# 3. 运行 docker-compose --env-file .env.docker up -d
#    Run: docker-compose --env-file .env.docker up -d

# ============================================
# 数据库配置 (必填)
# Database Configuration (Required)
# ============================================

# PostgreSQL 用户名
POSTGRES_USER=postgres

# PostgreSQL 密码 (请修改为强密码!)
# ⚠️ 必须修改此密码!
POSTGRES_PASSWORD=your-strong-password-here

# 数据库名称
POSTGRES_DB=noteapp

# PostgreSQL 端口 (默认 5432)
POSTGRES_PORT=5432

# ============================================
# 应用配置 (必填)
# Application Configuration (Required)
# ============================================

# 数据库模式: local 或 supabase
DATABASE_MODE=local

# 应用端口 (默认 3000)
APP_PORT=3000

# NextAuth 密钥 (请生成随机字符串!)
# 生成方法: openssl rand -base64 32
# ⚠️ 必须修改此密钥!
NEXTAUTH_SECRET=your-nextauth-secret-here

# Auth 密钥 (与 NEXTAUTH_SECRET 相同)
AUTH_SECRET=your-nextauth-secret-here

# 应用 URL (根据你的部署地址修改)
NEXTAUTH_URL=http://localhost:3000

# ============================================
# Supabase 配置 (可选 - 仅 supabase 模式需要)
# Supabase Configuration (Optional)
# ============================================

# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# AI 功能配置 (可选)
# AI Features Configuration (Optional)
# ============================================

# DeepSeek API 密钥
# DEEPSEEK_API_KEY=your-deepseek-api-key
