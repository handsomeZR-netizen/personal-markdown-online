# ============================================
# 本地开发环境配置示例
# Local Development Environment Configuration
# ============================================

# 数据库模式 (Database Mode)
# 选项: 'local' 或 'supabase'
# Options: 'local' or 'supabase'
DATABASE_MODE=local

# ============================================
# 本地 PostgreSQL 配置
# Local PostgreSQL Configuration
# ============================================

# 数据库连接 URL (Database Connection URL)
# 格式: postgresql://用户名:密码@主机:端口/数据库名
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/noteapp

# 直接连接 URL (Direct Connection URL)
# 用于迁移和管理操作
# Used for migrations and management operations
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/noteapp

# ============================================
# NextAuth 配置
# NextAuth Configuration
# ============================================

# NextAuth 密钥 (NextAuth Secret)
# 生成方法: openssl rand -base64 32
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-replace-with-random-string

# NextAuth URL
NEXTAUTH_URL=http://localhost:3000

# Auth 密钥 (Auth Secret)
# 应与 NEXTAUTH_SECRET 相同
# Should match NEXTAUTH_SECRET
AUTH_SECRET=your-secret-key-here-replace-with-random-string

# ============================================
# 本地存储配置
# Local Storage Configuration
# ============================================

# 本地文件存储路径 (Local File Storage Path)
LOCAL_STORAGE_PATH=./uploads

# ============================================
# 协作服务器配置
# Collaboration Server Configuration
# ============================================

# 协作服务器端口 (Collaboration Server Port)
COLLAB_SERVER_PORT=1234

# 协作服务器密钥 (Collaboration Server Secret)
COLLAB_SERVER_SECRET=local-dev-secret

# 协作服务器 URL (Collaboration Server URL)
NEXT_PUBLIC_COLLAB_SERVER_URL=ws://localhost:1234

# ============================================
# 可选: AI 功能配置
# Optional: AI Features Configuration
# ============================================

# DeepSeek API 密钥 (如果使用 AI 功能)
# DeepSeek API Key (if using AI features)
# DEEPSEEK_API_KEY=your-deepseek-api-key

# AI API 超时时间 (毫秒)
# AI API Timeout (milliseconds)
# AI_API_TIMEOUT=30000
