# ============================================
# 生产环境配置示例 (Supabase 模式)
# Production Environment Configuration (Supabase Mode)
# ============================================

# 数据库模式 (Database Mode)
# 选项: 'local' 或 'supabase'
# Options: 'local' or 'supabase'
DATABASE_MODE=supabase

# ============================================
# Supabase 配置
# Supabase Configuration
# ============================================

# Supabase 项目 URL (Supabase Project URL)
# 从 Supabase 项目设置中获取
# Get from Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase 匿名密钥 (Supabase Anonymous Key)
# 从 Supabase 项目设置 > API 中获取
# Get from Supabase project settings > API
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase 服务角色密钥 (Supabase Service Role Key)
# 从 Supabase 项目设置 > API 中获取
# Get from Supabase project settings > API
# ⚠️ 警告: 仅在服务器端使用,不要暴露给客户端
# ⚠️ Warning: Server-side only, do not expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# Supabase PostgreSQL 配置
# Supabase PostgreSQL Configuration
# ============================================

# 数据库连接 URL (Database Connection URL)
# 从 Supabase 项目设置 > Database > Connection string 中获取
# Get from Supabase project settings > Database > Connection string
# 使用 "Transaction" 模式的连接池 URL (端口 6543)
# Use "Transaction" pooling mode URL (port 6543)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true

# 直接连接 URL (Direct Connection URL)
# 用于迁移和管理操作 (端口 5432)
# Used for migrations and management operations (port 5432)
DIRECT_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ============================================
# NextAuth 配置
# NextAuth Configuration
# ============================================

# NextAuth 密钥 (NextAuth Secret)
# 生成方法: openssl rand -base64 32
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-production-secret-key-here

# NextAuth URL (生产环境域名)
# NextAuth URL (production domain)
NEXTAUTH_URL=https://your-domain.com

# Auth 密钥 (Auth Secret)
# 应与 NEXTAUTH_SECRET 相同
# Should match NEXTAUTH_SECRET
AUTH_SECRET=your-production-secret-key-here

# ============================================
# 协作服务器配置
# Collaboration Server Configuration
# ============================================

# 协作服务器端口 (Collaboration Server Port)
COLLAB_SERVER_PORT=1234

# 协作服务器密钥 (Collaboration Server Secret)
# 生成一个强密钥用于生产环境
# Generate a strong secret for production
COLLAB_SERVER_SECRET=your-production-collab-secret

# 协作服务器 URL (Collaboration Server URL)
# 使用生产环境的 WebSocket URL
# Use production WebSocket URL
NEXT_PUBLIC_COLLAB_SERVER_URL=wss://your-domain.com

# ============================================
# 可选: AI 功能配置
# Optional: AI Features Configuration
# ============================================

# DeepSeek API 密钥 (如果使用 AI 功能)
# DeepSeek API Key (if using AI features)
# DEEPSEEK_API_KEY=your-deepseek-api-key

# AI API 超时时间 (毫秒)
# AI API Timeout (milliseconds)
# AI_API_TIMEOUT=30000

# ============================================
# 部署平台配置 (如 Vercel)
# Deployment Platform Configuration (e.g., Vercel)
# ============================================

# Vercel 环境变量会自动设置以下值:
# Vercel automatically sets these environment variables:
# - VERCEL=1
# - VERCEL_ENV=production
# - VERCEL_URL=your-deployment-url.vercel.app

# ============================================
# 注意事项
# Important Notes
# ============================================

# 1. 替换所有 [YOUR-PASSWORD] 和 [YOUR-PROJECT-REF] 占位符
#    Replace all [YOUR-PASSWORD] and [YOUR-PROJECT-REF] placeholders
#
# 2. 确保所有密钥都是强随机字符串
#    Ensure all secrets are strong random strings
#
# 3. 不要将此文件提交到版本控制
#    Do not commit this file to version control
#
# 4. 在部署平台(如 Vercel)中设置这些环境变量
#    Set these environment variables in your deployment platform (e.g., Vercel)
